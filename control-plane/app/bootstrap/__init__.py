"""Local, non-HTTP bootstrap entrypoints.

These commands are deployment orchestration boundaries.  They do not create
schemas, advertise public intents, or bypass the PostgreSQL authority model.
"""
