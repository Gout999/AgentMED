# Commands index

Exact commands, working directories, exit codes, counts, and summarized outputs are recorded in
[`../raw/commands.txt`](../raw/commands.txt). The contract suites were executed only from the
detached clean checkout bound to the subject commit.
