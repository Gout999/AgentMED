# Runtime artifacts not applicable

D1 freezes decision and contract semantics only. It does not build a migration, database schema,
runtime service, HTTP/CLI activation path, container image, provider output, Agent trace, repository
sandbox result, external side effect, or production artifact.
