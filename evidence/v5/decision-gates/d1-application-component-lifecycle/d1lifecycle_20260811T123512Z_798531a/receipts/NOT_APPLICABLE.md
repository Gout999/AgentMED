# Receipts not applicable

D1 is a contract decision gate. It performs no business mutation and creates no runtime
AuthorityReceipt, event, audit, outbox, provider, Agent, repository, external-operation, or
production receipt. Receipt-dependent facets remain `NOT_RUN`.
