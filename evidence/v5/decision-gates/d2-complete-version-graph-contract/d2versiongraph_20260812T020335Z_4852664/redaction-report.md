# D2 redaction report

No secret values were recorded. The semantic series contains contract YAML/JSON
schemas, conformance tests and a fixture only; no credentials, tokens, keys,
provider secrets or PII were added (cumulative added-line scan: 0 findings).
Runtime identities were not exercised (facets NOT_RUN).
