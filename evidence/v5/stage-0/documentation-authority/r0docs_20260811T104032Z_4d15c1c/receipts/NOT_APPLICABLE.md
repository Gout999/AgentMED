# Receipts: not applicable

R0 is a documentation-authority stage. It did not execute a business command, mutate PostgreSQL,
dispatch work, call a provider or perform an external action, so no authority, audit, operation,
provider or human-authorization receipt exists. All runtime evidence facets remain `NOT_RUN`.
