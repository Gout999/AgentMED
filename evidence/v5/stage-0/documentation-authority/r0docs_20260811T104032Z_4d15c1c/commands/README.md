# Command evidence

R0 used static, read-only commands in a detached clean checkout. The exact command labels,
working-directory class, exit codes and result counts are recorded in
[`../raw/commands.txt`](../raw/commands.txt). No runtime, database, container, provider or external
command was executed.
