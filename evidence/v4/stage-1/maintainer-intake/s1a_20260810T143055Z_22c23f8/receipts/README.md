# Receipts

This package contains local contract, replay and disposable-PostgreSQL evidence only. The PostgreSQL integration persisted and revalidated Stage 1A domain records, audits, outbox rows and AuthorityReceipts before guarded cleanup.

No provider, AgentTeams, Claude runtime, repository write, human-authorized external operation or production receipt exists for this run. Those facets are `NOT_RUN` and must not be inferred from local success.
